define(['backbone'], function (BackBone) {
    "use strict";
    var Address = BackBone.Model.extend({
        initialize: function () {
        },
        validate: function (atts, ops) {}

    });
    return Address;
});
